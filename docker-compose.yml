networks:
  pickside-network:
    external: true

services:
  authsvc:
    build: .
    container_name: authsvc
    ports:
      - "8081:8081"
    networks:
      - pickside-network
    environment:
      - VAULT_ADDRESS=https://vault.morpheglus.org
      - VAULT_TOKEN=${VAULT_TOKEN}